---
openapi: "3.0.2"
info:
  description: Viper Boot API documentation
  title: Viper Boot API
  #version: X.X.X (this will be replaced by latest version tag of git generated by commitizen)
servers:
  - url: http://{host}:{port}/
    description: Viper Boot API server
    variables:
      host:
        enum:
          - "127.0.0.1"
          - "localhost"
        default: "127.0.0.1"
      port:
        enum:
          - 3000
        default: 3000
