"""Gender Enum."""
from enum import Enum


class GenderEnum(Enum):
    """Valid names for Gender."""

    MALE = "male"
    """Valid name for Male gender"""
    FEMALE = "female"
    """Valid name for Female gender"""
